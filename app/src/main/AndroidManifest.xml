<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gmail.bodziowaty6978.kodyzabka">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".CodeApp"
        android:allowBackup="true"
        android:icon="@drawable/ic_frog_svgrepo_com"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_frog_svgrepo_com"
        android:supportsRtl="true"
        android:theme="@style/Theme.KodyZabka">
        <activity
            android:name=".feature_code.presentation.codes_list.CodesListActivity"
            android:exported="false" />
        <activity
            android:name=".feature_code.presentation.add_edit_code.AddEditCodeActivity"
            android:exported="false" />
        <activity
            android:name=".feature_code.presentation.codes.CodeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>